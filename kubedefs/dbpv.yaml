apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  labels: 
    app: tododb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
  hostPath:
    path: /mnt/data/db
  

